import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.arcade-cabinet.psyduck",
  appName: "Psyduck's Infinite Headache",
  webDir: "dist",
};

export default config;
